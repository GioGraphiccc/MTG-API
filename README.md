# MTG-API
Personal project.
 
 
 1. Create virtual python env using:
  > python -m venv venv
  
  
2. Open cmd and type:
  > powershell
  
  > venv\Scripts\activate.ps1
  
  > streamlit run MTGView.py
